{
  "name": "openzeppelin-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-ganache": "ganache-cli --deterministic -p 8545 -m \"tumble gas embody bright agree pony smoke laptop index sight shallow hungry\"",
    "lint": "node_modules/.bin/solhint \"contracts/**/*.sol\"",
    "test": "node_modules/.bin/truffle test --network development",
    "test-coverage": "solidity-coverage",
    "complie": "yarn lint && node_modules/.bin/truffle complie",
    "deploy": "yarn lint && node_modules/.bin/truffle migrate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "truffle": "^5.0.1",
    "truffle-hdwallet-provider": "^1.0.17",
    "zos": "^2.1.0",
    "zos-lib": "^2.1.0"
  },
  "devDependencies": {
    "ganache-cli": "^6.10.2"
  }
}
